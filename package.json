{
  "name": "ombrage-api",
  "module": "src/index.ts",
  "repository": {
    "url": "https://git.ombrage.space/salokain/bun-api-framework"
  },
  "version": "1.5.0",
  "packageManager": "bun@1.3.0",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "lint:fix": "biome check --write .",
    "lint": "biome check",
    "prepare": "husky",
    "check": "tsc --noEmit",
    "test": "bun test --coverage",
    "generate:env-docs": "bun run ./scripts/generate-env.ts",
    "build:js": "bun run ./build.ts",
    "build:types": "tsc --outDir dist && tsc-alias --outDir dist",
    "build:clean": "rm -rf dist",
    "build": "bun run build:clean && bun run build:js && bun run build:types",
    "publish": "changelogen --release && bun publish --tag latest --ignore-scripts && git push && git push --tags"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.2",
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@types/bun": "^1.3.1",
    "changelogen": "^0.6.2",
    "husky": "^9.1.7",
    "openapi-types": "^12.1.3",
    "tsc-alias": "^1.8.16",
    "zod": "^4.1.12"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {}
}
