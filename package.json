{
  "name": "ombrage-api",
  "module": "src/index.ts",
  "repository": {
    "url": "https://github.com/boyer-nicolas/ombrage-api"
  },
  "license": "MIT",
  "version": "0.0.1",
  "packageManager": "bun@1.3.0",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "bun run --hot dev/index.ts",
    "lint:fix": "biome check --write .",
    "lint": "biome check",
    "prepare": "husky",
    "check": "tsc --noEmit",
    "test": "bun test",
    "test:coverage": "bun run ./scripts/test-with-coverage.ts",
    "coverage:badge": "bun run ./scripts/generate-coverage-badge.ts",
    "coverage:report": "bun run ./scripts/generate-coverage-report.ts",
    "generate:env-docs": "bun run ./scripts/generate-env.ts",
    "build:js": "bun run ./build.ts",
    "build:types": "tsc --outDir dist && tsc-alias --outDir dist",
    "build:clean": "rm -rf dist",
    "build": "bun run build:clean && bun run build:js && bun run build:types",
    "publish:local": "bun run ./scripts/publish.ts",
    "publish": "changelogen --release --bump && bun publish --tag latest --ignore-scripts && git push && git push --tags"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.2",
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@types/bun": "^1.3.1",
    "changelogen": "^0.6.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "openapi-types": "^12.1.3",
    "tsc-alias": "^1.8.16",
    "zod": "^4.1.12"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {}
}
